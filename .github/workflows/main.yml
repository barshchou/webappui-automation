name: Cypress not full reports tests

on:
  workflow_dispatch:
    inputs:
      choice: 
        type: choice
        description: Select the environment
        options:
        - dev
        - staging
        - prod
        required: true
        default: staging
  pull_request:
    branches: [ master ]

jobs:
  Not-full-reports-run:
    runs-on: ubuntu-latest
    container: cypress/browsers:node16.5.0-chrome94-ff93
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Cypress run
        uses: cypress-io/github-action@v2
        with:
          record: true
          browser: chrome
          spec: cypress/integration/not_full_reports/income/residential/in_place_rent_roll/QA-4524.spec.js
          tag: ${{github.event.inputs.choice}}
        env:
          CYPRESS_USERNAME: ${{ secrets.USERNAME }}
          CYPRESS_PASSWORD: ${{ secrets.PASSWORD }}
          CYPRESS_RECORD_KEY: ${{ secrets.TEST_LAUNCHES_RECORD_KEY }}
          CYPRESS_url: ${{ github.event.inputs.choice }}
