const rowsMarketAdjustment = {
    sale_date: "Sale Date",
    cap_rate: "Cap Rate",
    price_per_unit: "Price per Unit",
    price_per_sf: "Price per SF",
    property_description: "Property Description"
} as const;

export default Object.freeze(rowsMarketAdjustment);