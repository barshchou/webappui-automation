const discussionSections = {
    unitOfComparison: "Unit of Comparison",
    market: "Market",
    location: "Location",
    utility: "Utility",
    other: "Other"
} as const;

export default Object.freeze(discussionSections);