const unitsOfMeasure = {
    annually: "annually",
    monthly: "monthly",
    perSquareFootPerYear: "per square foot per year",
    perSquareFootPerMonth: "per square foot per month"
} as const;

export default Object.freeze(unitsOfMeasure);