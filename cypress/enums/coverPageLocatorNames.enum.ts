const coverPageLocatorNames = {
    buildingName: "building-name",
    applicationNumber: "application-number",
    clientName: "client-name",
    title: "title",
    client: "client",
    address: "address",
    city: "city"
} as const;

export default Object.freeze(coverPageLocatorNames);